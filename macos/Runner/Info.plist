<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIconFile</key>
	<string></string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>NSHumanReadableCopyright</key>
	<string>$(PRODUCT_COPYRIGHT)</string>
	<key>NSMainNibFile</key>
	<string>MainMenu</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<key>LSUIElement</key>
	<true/>
	<!-- Opt OUT of Flutter's merged UI+platform thread (the engine default since
	     Flutter 3.35; pinned here at 3.41.6 via .fvmrc). With the threads merged,
	     window_manager / tray_manager do blocking platform-channel work during
	     main() that races into a deadlock on the single shared thread: the engine
	     prints "Running with merged UI and platform thread. Experimental." and
	     then the app hangs at launch with no status item and no window (it never
	     gets far enough to create the tray) — exactly the "installed it, won't
	     open, no icon" failure. Forcing the classic separate-thread model
	     restores a reliable launch. (Flutter warns this opt-out will be removed
	     eventually; revisit once the menu-bar plugins are merged-thread safe.) -->
	<key>FLTEnableMergedPlatformUIThread</key>
	<false/>
	<!-- Sparkle EdDSA public key. The matching PRIVATE key lives in the login
	     Keychain (search "sparkle"); back it up — it cannot be regenerated.
	     See the release skill for the export/import runbook. The feed URL is set
	     at runtime via autoUpdater.setFeedURL() in lib/main.dart (no SUFeedURL). -->
	<key>SUPublicEDKey</key>
	<string>OQCMVAs9l5UXNdwz3Fd2GBxig2oZn81BCdpZWt2/3Mo=</string>
</dict>
</plist>
